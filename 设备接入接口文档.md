# 设备接入接口文档（FoodFridge 留样柜）

## 基础信息

- **Base URL**：由前端/运维人员配置在 Android 端的 `API_BASE_URL` 中
- **认证方式**：HTTP Header `api-key`
- **请求格式**：纯 JSON，**不包含** `{ code, message, data }` 包装
- **响应格式**：统一包装为 `{ code, message, data }`

## 认证方式

所有接口均需在请求头中携带设备注册时分配的 API Key：

```http
api-key: {device_api_key}
```

> `api-key` 与 `device_id` 由设备注册/配置流程提供，Android 端当前以 `BuildConfig.API_DEVICE_KEY` 占位，待后端审查并补充真实来源后再替换。

---

## 接口列表

### 1. 留样上报

#### 接口地址

```http
POST /api/device/sampling/upload
```

#### 请求体

```json
{
  "device_id": "RK3568_001",
  "timestamp": 1741325400000,
  "dish_name": "红烧肉",
  "stall_name": "A区一食堂",
  "operator_name": "张三",
  "weight": 152.6
}
```

#### 请求字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| device_id | string | 是 | 设备编码 |
| timestamp | long | 是 | 留样时间戳（毫秒） |
| dish_name | string | 是 | 菜品名称 |
| stall_name | string | 否 | 档口名称 |
| operator_name | string | 否 | 操作员姓名 |
| weight | float | 是 | 留样克重（g） |

#### 响应体

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "recordId": "REC-20241229-002",
    "processTime": 89
  }
}
```

#### 响应字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| code | int | 业务状态码 |
| message | string | 提示信息 |
| data.recordId | string | 平台记录 ID |
| data.processTime | int | 处理耗时（毫秒） |

---

### 2. 温度上报

#### 接口地址

```http
POST /api/device/temperature/upload
```

#### 请求体

```json
{
  "device_id": "RK3568_001",
  "timestamp": 1741325400000,
  "temperature": 6.8
}
```

#### 请求字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| device_id | string | 是 | 设备编码 |
| timestamp | long | 是 | 采集时间戳（毫秒） |
| temperature | float | 是 | 柜内温度（℃） |

#### 响应体

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "recordId": "REC-20241229-003",
    "processTime": 23
  }
}
```

#### 响应字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| code | int | 业务状态码 |
| message | string | 提示信息 |
| data.recordId | string | 平台记录 ID |
| data.processTime | int | 处理耗时（毫秒） |

---

### 3. 开关门记录上报

#### 接口地址

```http
POST /api/device/door/record
```

#### 请求体

```json
{
  "device_id": "RK3568_001",
  "timestamp": 1741325410000,
  "operator": "张三",
  "open_time": "2024-12-29T12:34:50",
  "close_time": "2024-12-29T12:35:10"
}
```

#### 请求字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| device_id | string | 是 | 设备编码 |
| timestamp | long | 是 | 上报时间戳（毫秒），建议取关门时间 |
| operator | string | 否 | 开门人/操作员姓名 |
| open_time | string | 是 | 开门时间，ISO 8601 格式（本地时间，不带时区） |
| close_time | string | 是 | 关门时间，ISO 8601 格式（本地时间，不带时区） |

#### 响应体

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "recordId": "REC-20241229-004",
    "processTime": 31
  }
}
```

#### 响应字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| code | int | 业务状态码 |
| message | string | 提示信息 |
| data.recordId | string | 平台记录 ID |
| data.processTime | int | 处理耗时（毫秒） |

---

## 通用响应格式

所有接口返回统一格式：

```json
{
  "code": 200,
  "message": "success",
  "data": { ... }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| code | int | 业务状态码 |
| message | string | 提示信息 |
| data | object / null | 业务数据 |

## 错误码

| HTTP 状态码 | 业务码 | 说明 |
|------------|--------|------|
| 200 | 200 | 成功 |
| 400 | 40000 | 参数错误 |
| 401 | 40100 | API Key 无效或缺失 |
| 403 | 40302 | 设备维护中 |
| 404 | 40401 | 设备不存在 |
| 500 | 50001 | 服务器内部错误 |

## 备注

1. **请求体不再使用 `code/message/data` 包装**，只携带通用字段 `device_id`、`timestamp` 与各接口业务字段。
2. **Base URL 不复用独立的写死地址**，由前端配置人员维护在 `API_BASE_URL` 中，与现有业务接口共用同一域名。
3. 留样上报字段与第 4 节“智能留样称”对齐，仅去掉图片字段（无 `photo` / `photo_url`）。
4. 留样上报**不包含图片字段**；后端无需返回 `photo_url`。
5. 开关门记录在一次完整的「开门 → 关门」事件结束后上报。
6. Android 端已实现 `DeviceUploadApiService` 中的四个接口，后端审查通过并补充 `device_id`/`api-key` 来源后即可联调。
