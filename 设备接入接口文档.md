# 设备接入接口文档（留样柜）

## 基础信息

- **Base URL**：`https://device-api.example.com/`（待定，由平台提供后填入）
- **认证方式**：HTTP Header `api-key`
- **数据格式**：JSON
- **请求/响应统一包装**：`{ code, message, data }`

## 认证方式

所有接口均需在请求头中携带设备注册时分配的 API Key：

```http
api-key: {device_api_key}
```

> 当前 `api-key` 与 `device_id` 由设备注册/配置流程提供，Android 端以占位符实现，待后端审查后替换为真实值。

---

## 接口列表

### 1. 心跳保活

#### 接口地址

```http
POST /api/device/refresh
```

#### 请求体

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "device_id": "RK3568_001",
    "timestamp": 1741325400000
  }
}
```

#### 请求字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| code | int | 是 | 固定 200 |
| message | string | 是 | 固定 "success" |
| data.device_id | string | 是 | 设备编码 |
| data.timestamp | long | 是 | 心跳时间戳（毫秒） |

#### 响应体

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "processTime": 12
  }
}
```

#### 响应字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| code | int | 业务状态码 |
| message | string | 提示信息 |
| data.processTime | int | 处理耗时（毫秒） |

---

### 2. 留样上报

#### 接口地址

```http
POST /api/device/sampling/upload
```

#### 请求体

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "device_id": "RK3568_001",
    "timestamp": 1741325400000,
    "dish_name": "红烧肉",
    "stall_name": "A区一食堂",
    "operator_name": "张三",
    "weight": 152.6,
    "photo": "/9j/4AAQSkZJRgABAQAAAQABAAD..."
  }
}
```

#### 请求字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| code | int | 是 | 固定 200 |
| message | string | 是 | 固定 "success" |
| data.device_id | string | 是 | 设备编码 |
| data.timestamp | long | 是 | 留样时间戳（毫秒） |
| data.dish_name | string | 是 | 菜品名称 |
| data.stall_name | string | 否 | 档口名称 |
| data.operator_name | string | 否 | 操作员姓名 |
| data.weight | float | 是 | 留样克重（g） |
| data.photo | string | 否 | 留样抓拍图片的 Base64 编码 |

#### 响应体

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "recordId": "REC-20241229-002",
    "processTime": 89
  }
}
```

#### 响应字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| code | int | 业务状态码 |
| message | string | 提示信息 |
| data.recordId | string | 平台记录 ID |
| data.processTime | int | 处理耗时（毫秒） |

---

## 通用响应格式

所有接口返回统一格式：

```json
{
  "code": 200,
  "message": "success",
  "data": { ... }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| code | int | 业务状态码 |
| message | string | 提示信息 |
| data | object / null | 业务数据 |

## 错误码

| HTTP 状态码 | 业务码 | 说明 |
|------------|--------|------|
| 200 | 200 | 成功 |
| 400 | 40000 | 参数错误 |
| 401 | 40100 | API Key 无效或缺失 |
| 404 | 40401 | 设备不存在 |
| 500 | 50001 | 服务器内部错误 |

## 备注

1. 请求体采用通用格式包装（`code/message/data`），设备单条业务信息放在 `data` 中。
2. `stall_id` / `dish_id` 由设备本地自生成，平台上报接口只接收名称，不做 ID 映射。
3. 图片字段 `photo` 为 Base64 字符串，可选。
4. 心跳接口建议由设备定时调用（例如每 30 秒一次）。
5. Android 端已按本文档实现 `DeviceUploadApiService`，后端审查通过并补充 Base URL、`api-key`、`device_id` 来源后即可联调。
